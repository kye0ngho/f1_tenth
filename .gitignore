# 빌드 산출물 — 컨테이너에서 생성, 절대경로가 박혀 호스트에선 무의미
build/
install/
log/

# 주행 데이터(rosbag/로그) — 수백 MB.
# 선행 슬래시로 루트에만 적용한다. 이게 없으면 ROS 패키지인
# autonomous/src/data/ 까지 같이 무시되어 맵과 웨이포인트가 통째로 빠진다.
/data/

# 파이썬
__pycache__/
*.py[cod]
*.egg-info/
