diff --git a/vesc_ackermann/include/vesc_ackermann/vesc_to_odom.hpp b/vesc_ackermann/include/vesc_ackermann/vesc_to_odom.hpp
index 60eb3a7..9cb7a8d 100644
--- a/vesc_ackermann/include/vesc_ackermann/vesc_to_odom.hpp
+++ b/vesc_ackermann/include/vesc_ackermann/vesc_to_odom.hpp
@@ -33,8 +33,10 @@
 
 #include <tf2_ros/transform_broadcaster.h>
 
+#include <deque>
 #include <memory>
 #include <string>
+#include <utility>
 
 #include <nav_msgs/msg/odometry.hpp>
 #include <rclcpp/rclcpp.hpp>
@@ -63,11 +65,13 @@ private:
   double speed_to_erpm_gain_, speed_to_erpm_offset_;
   double steering_to_servo_gain_, steering_to_servo_offset_;
   double wheelbase_;
+  double servo_command_delay_;
   bool publish_tf_;
 
   // odometry state
   double x_, y_, yaw_;
-  Float64::SharedPtr last_servo_cmd_;  ///< Last servo position commanded value
+  /// Timestamped servo commands used to model steering actuator latency.
+  std::deque<std::pair<rclcpp::Time, double>> servo_cmd_history_;
   VescStateStamped::SharedPtr last_state_;  ///< Last received state message
 
   // ROS services
diff --git a/vesc_ackermann/src/vesc_to_odom.cpp b/vesc_ackermann/src/vesc_to_odom.cpp
index 83a11fb..612c5fb 100644
--- a/vesc_ackermann/src/vesc_to_odom.cpp
+++ b/vesc_ackermann/src/vesc_to_odom.cpp
@@ -69,6 +69,7 @@ VescToOdom::VescToOdom(const rclcpp::NodeOptions & options)
     steering_to_servo_offset_ =
       declare_parameter<double>("steering_angle_to_servo_offset");
     wheelbase_ = declare_parameter<double>("wheelbase");
+    servo_command_delay_ = declare_parameter<double>("servo_command_delay", 0.0);
   }
 
   publish_tf_ = declare_parameter("publish_tf", publish_tf_);
@@ -93,20 +94,32 @@ VescToOdom::VescToOdom(const rclcpp::NodeOptions & options)
 
 void VescToOdom::vescStateCallback(const VescStateStamped::SharedPtr state)
 {
-  // check that we have a last servo command if we are depending on it for angular velocity
-  if (use_servo_cmd_ && !last_servo_cmd_) {
+  // Check that we have a servo command if we depend on it for angular velocity.
+  if (use_servo_cmd_ && servo_cmd_history_.empty()) {
     return;
   }
 
   // convert to engineering units
-  double current_speed = (-state->state.speed - speed_to_erpm_offset_) / speed_to_erpm_gain_;
+  double current_speed = (state->state.speed - speed_to_erpm_offset_) / speed_to_erpm_gain_;
   if (std::fabs(current_speed) < 0.05) {
     current_speed = 0.0;
   }
   double current_steering_angle(0.0), current_angular_velocity(0.0);
   if (use_servo_cmd_) {
+    const rclcpp::Time state_time(state->header.stamp);
+    const auto delayed_time = state_time - rclcpp::Duration::from_seconds(servo_command_delay_);
+
+    // Keep the newest command not newer than the actuator-delay-adjusted state time.
+    while (
+      servo_cmd_history_.size() > 1 &&
+      servo_cmd_history_[1].first <= delayed_time)
+    {
+      servo_cmd_history_.pop_front();
+    }
+
+    const double delayed_servo_cmd = servo_cmd_history_.front().second;
     current_steering_angle =
-      (last_servo_cmd_->data - steering_to_servo_offset_) / steering_to_servo_gain_;
+      (delayed_servo_cmd - steering_to_servo_offset_) / steering_to_servo_gain_;
     current_angular_velocity = current_speed * tan(current_steering_angle) / wheelbase_;
   }
 
@@ -182,7 +195,16 @@ void VescToOdom::vescStateCallback(const VescStateStamped::SharedPtr state)
 
 void VescToOdom::servoCmdCallback(const Float64::SharedPtr servo)
 {
-  last_servo_cmd_ = servo;
+  servo_cmd_history_.emplace_back(now(), servo->data);
+
+  // The calibrated delay is 0.13 s; one second is ample history and bounds memory use.
+  const auto oldest_allowed = now() - rclcpp::Duration::from_seconds(1.0);
+  while (
+    servo_cmd_history_.size() > 1 &&
+    servo_cmd_history_[1].first < oldest_allowed)
+  {
+    servo_cmd_history_.pop_front();
+  }
 }
 
 }  // namespace vesc_ackermann
